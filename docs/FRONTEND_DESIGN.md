# 飞马星球医院预约挂号系统 - 前端设计文档

## 一、技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.x | 前端框架 |
| Vite | 5.x | 构建工具 |
| Ant Design | 5.x | UI组件库 |
| TailwindCSS | 3.x | 样式工具 |
| React Router | 6.x | 路由管理 |
| Axios | 1.x | HTTP请求 |
| Zustand | 4.x | 状态管理 |
| Day.js | 1.x | 日期处理 |

---

## 二、页面结构

### 2.1 页面路由设计

```
/                           # 首页（科室展示）
├── /login                  # 统一登录页
├── /register               # 患者注册页
│
├── /patient                # 患者功能区
│   ├── /patient/profile    # 个人信息
│   ├── /patient/appointments  # 我的预约
│   └── /patient/book       # 预约挂号
│
├── /doctor                 # 医生功能区（可选）
│   ├── /doctor/schedule    # 我的排班
│   └── /doctor/patients    # 预约患者列表
│
└── /admin                  # 管理后台
    ├── /admin/doctors      # 医生管理
    ├── /admin/schedules    # 排班管理
    ├── /admin/import       # 数据导入
    └── /admin/reports      # 报告导出
```

### 2.2 目录结构

```
frontend/
├── index.html
├── package.json
├── vite.config.js
├── tailwind.config.js
├── public/
│   └── logo.png
└── src/
    ├── main.jsx                 # 入口文件
    ├── App.jsx                  # 根组件（路由配置）
    │
    ├── api/                     # API请求封装
    │   ├── index.js             # Axios实例和拦截器
    │   ├── patient.js           # 患者相关API
    │   ├── doctor.js            # 医生相关API
    │   ├── department.js        # 科室相关API
    │   ├── schedule.js          # 排班相关API
    │   ├── appointment.js       # 预约相关API
    │   └── admin.js             # 管理相关API
    │
    ├── components/              # 公共组件
    │   ├── Layout/              # 布局组件
    │   │   ├── MainLayout.jsx   # 主布局（患者端）
    │   │   └── AdminLayout.jsx  # 管理端布局
    │   ├── Header.jsx           # 头部导航
    │   ├── Footer.jsx           # 页脚
    │   └── Loading.jsx          # 加载组件
    │
    ├── pages/                   # 页面组件
    │   ├── Home/                # 首页
    │   │   └── index.jsx
    │   ├── Login/               # 登录页
    │   │   └── index.jsx
    │   ├── Register/            # 注册页
    │   │   └── index.jsx
    │   ├── Patient/             # 患者功能
    │   │   ├── Profile.jsx      # 个人信息
    │   │   ├── Appointments.jsx # 我的预约
    │   │   └── Book.jsx         # 预约挂号
    │   └── Admin/               # 管理功能
    │       ├── Doctors.jsx      # 医生管理
    │       ├── Schedules.jsx    # 排班管理
    │       ├── Import.jsx       # 数据导入
    │       └── Reports.jsx      # 报告导出
    │
    ├── store/                   # 状态管理
    │   └── userStore.js         # 用户状态
    │
    ├── utils/                   # 工具函数
    │   ├── validator.js         # 表单验证
    │   └── format.js            # 格式化工具
    │
    └── styles/                  # 全局样式
        └── global.css
```

---

## 三、页面设计详情

### 3.1 首页 (`/`)

**功能**：展示医院科室信息，引导用户预约

**UI元素**：
- 顶部导航栏：Logo、登录/注册按钮
- 轮播图：医院介绍
- 科室卡片列表：点击进入该科室医生列表
- 快速预约入口

**数据接口**：
- `GET /api/department/list` - 获取科室列表

**示意图**：
```
┌────────────────────────────────────────────────────────┐
│  🏥 飞马星球医院              [登录] [注册]            │
├────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────┐  │
│  │              欢迎来到飞马星球医院                 │  │
│  │           专业、高效、贴心的医疗服务             │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  选择科室                                              │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐      │
│  │  内科   │ │  外科   │ │  儿科   │ │ 妇产科  │      │
│  │  🫀     │ │  🔪     │ │  👶     │ │  🤰     │      │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘      │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐      │
│  │  眼科   │ │ 耳鼻喉  │ │ 口腔科  │ │ 皮肤科  │      │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘      │
└────────────────────────────────────────────────────────┘
```

---

### 3.2 登录页 (`/login`)

**功能**：患者/医生/管理员统一登录

**UI元素**：
- 用户类型选择（Tab切换）
- 账号输入框
- 密码输入框
- 登录按钮
- 跳转注册链接

**表单验证**：
- 账号不能为空
- 密码不能为空

**数据接口**：
- `POST /api/patient/login` - 患者登录
- `POST /api/doctor/login` - 医生登录
- `POST /api/admin/login` - 管理员登录

**示意图**：
```
┌────────────────────────────────────────────────────────┐
│                                                        │
│                   🏥 飞马星球医院                      │
│                                                        │
│         ┌─────────────────────────────────┐           │
│         │  [患者]  [医生]  [管理员]        │           │
│         ├─────────────────────────────────┤           │
│         │                                 │           │
│         │  账号: [__________________]     │           │
│         │                                 │           │
│         │  密码: [__________________]     │           │
│         │                                 │           │
│         │       [      登 录      ]       │           │
│         │                                 │           │
│         │  还没有账号？立即注册 →         │           │
│         └─────────────────────────────────┘           │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

### 3.3 注册页 (`/register`)

**功能**：新患者注册账号

**UI元素**：
- 姓名输入框
- 身份证号输入框
- 手机号输入框
- 性别选择
- 密码输入框
- 确认密码输入框
- 注册按钮

**表单验证**：
- 姓名：必填，最多20字符
- 身份证号：必填，18位数字，年龄≥10岁
- 手机号：必填，11位手机号格式
- 性别：必选
- 密码：必填，不少于4位
- 确认密码：必须与密码一致

**数据接口**：
- `POST /api/patient/register`

---

### 3.4 预约挂号页 (`/patient/book`)

**功能**：选择科室→选择医生→选择时间段→确认预约

**流程步骤**：

**Step 1: 选择科室**
- 展示科室卡片列表
- 点击选择科室

**Step 2: 选择医生**
- 展示该科室医生列表
- 显示医生姓名、专长
- 点击选择医生

**Step 3: 选择时间**
- 展示医生排班日历
- 显示可用号源数量
- 选择具体时间段

**Step 4: 确认预约**
- 显示预约信息汇总
- 确认按钮

**数据接口**：
- `GET /api/department/list` - 获取科室
- `GET /api/doctor/department/{departmentId}` - 获取科室医生
- `GET /api/doctor/{doctorId}/schedule` - 获取医生排班
- `POST /api/appointment` - 创建预约

**示意图**：
```
┌────────────────────────────────────────────────────────┐
│  预约挂号                                              │
│                                                        │
│  步骤: ① 选择科室 → ② 选择医生 → ③ 选择时间 → ④ 确认  │
├────────────────────────────────────────────────────────┤
│                                                        │
│  选择医生（内科）                     [返回选择科室]   │
│                                                        │
│  ┌────────────────────────────────────────────────┐   │
│  │  👨‍⚕️ 张三                                       │   │
│  │  专长：擅长呼吸系统疾病、慢性咳嗽、肺炎的诊治   │   │
│  │                                    [选择]      │   │
│  └────────────────────────────────────────────────┘   │
│  ┌────────────────────────────────────────────────┐   │
│  │  👨‍⚕️ 李四                                       │   │
│  │  专长：擅长消化系统疾病、胃肠道疾病的诊治       │   │
│  │                                    [选择]      │   │
│  └────────────────────────────────────────────────┘   │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

### 3.5 我的预约页 (`/patient/appointments`)

**功能**：查看预约记录，取消预约

**UI元素**：
- 预约状态筛选Tab（全部/已预约/已完成/已取消）
- 预约记录列表
- 每条记录显示：预约号、医生、科室、时间、状态
- 取消按钮（仅"已预约"状态显示）

**数据接口**：
- `GET /api/appointment/patient/{patientId}` - 获取预约列表
- `PUT /api/appointment/{id}/cancel` - 取消预约

**示意图**：
```
┌────────────────────────────────────────────────────────┐
│  我的预约                                              │
│                                                        │
│  [全部] [已预约] [已完成] [已取消]                     │
├────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────┐   │
│  │  预约号：000000000001                          │   │
│  │  医生：张三（内科）                            │   │
│  │  时间：2025-12-12 08:00                        │   │
│  │  状态：🟢 已预约                    [取消预约] │   │
│  └────────────────────────────────────────────────┘   │
│  ┌────────────────────────────────────────────────┐   │
│  │  预约号：000000000002                          │   │
│  │  医生：王五（外科）                            │   │
│  │  时间：2025-12-10 14:00                        │   │
│  │  状态：✅ 已完成                               │   │
│  └────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────┘
```

---

### 3.6 个人信息页 (`/patient/profile`)

**功能**：查看和修改个人信息

**UI元素**：
- 信息展示卡片
- 编辑按钮
- 编辑表单（姓名、手机号、性别可修改；ID、身份证号不可修改）
- 注销账号按钮

**数据接口**：
- `GET /api/patient/{patientId}` - 获取信息
- `PUT /api/patient/{patientId}` - 修改信息
- `DELETE /api/patient/{patientId}` - 注销账号

---

### 3.7 管理后台 - 医生管理 (`/admin/doctors`)

**功能**：查看、添加、编辑、删除医生

**UI元素**：
- 医生列表表格
- 添加医生按钮
- 编辑/删除操作按钮
- 添加/编辑弹窗表单

**数据接口**：
- `GET /api/doctor/list` - 获取医生列表
- `POST /api/doctor` - 添加医生
- `PUT /api/doctor/{id}` - 更新医生
- `DELETE /api/doctor/{id}` - 删除医生

---

### 3.8 管理后台 - 数据导入 (`/admin/import`)

**功能**：批量导入医生和排班数据

**UI元素**：
- 下载模板按钮（医生模板、排班模板）
- 上传区域（拖拽或选择文件）
- 导入类型选择
- 导入结果提示

**数据接口**：
- `GET /api/admin/template/doctor` - 下载医生模板
- `GET /api/admin/template/schedule` - 下载排班模板
- `POST /api/admin/import/doctor` - 导入医生
- `POST /api/admin/import/schedule` - 导入排班

---

### 3.9 管理后台 - 报告导出 (`/admin/reports`)

**功能**：导出预约记录和生成统计报告

**UI元素**：
- 导出预约记录按钮（Excel）
- 月份选择器
- 生成月度报告按钮（PDF）

**数据接口**：
- `GET /api/admin/export/appointments` - 导出预约Excel
- `GET /api/admin/report/monthly?year=&month=` - 生成PDF报告

---

## 四、状态管理

### 4.1 用户状态 (userStore.js)

```javascript
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

const useUserStore = create(
    persist(
        (set) => ({
            // 状态
            user: null,           // 用户信息
            userType: null,       // 用户类型: patient, doctor, admin
            isLoggedIn: false,    // 登录状态
            
            // 操作
            login: (user, userType) => set({
                user,
                userType,
                isLoggedIn: true
            }),
            
            logout: () => set({
                user: null,
                userType: null,
                isLoggedIn: false
            }),
            
            updateUser: (userData) => set((state) => ({
                user: { ...state.user, ...userData }
            }))
        }),
        {
            name: 'user-storage'  // localStorage key
        }
    )
);

export default useUserStore;
```

---

## 五、通用组件

### 5.1 主布局组件

```jsx
// components/Layout/MainLayout.jsx
const MainLayout = ({ children }) => {
    return (
        <div className="min-h-screen flex flex-col">
            <Header />
            <main className="flex-1 container mx-auto px-4 py-6">
                {children}
            </main>
            <Footer />
        </div>
    );
};
```

### 5.2 路由守卫

```jsx
// components/AuthRoute.jsx
const AuthRoute = ({ children, allowedTypes }) => {
    const { isLoggedIn, userType } = useUserStore();
    const navigate = useNavigate();
    
    useEffect(() => {
        if (!isLoggedIn) {
            navigate('/login');
        } else if (allowedTypes && !allowedTypes.includes(userType)) {
            navigate('/');
        }
    }, [isLoggedIn, userType]);
    
    return isLoggedIn ? children : null;
};
```

---

## 六、UI设计规范

### 6.1 颜色规范

| 用途 | 颜色 | Tailwind类 |
|------|------|------------|
| 主色 | #1890ff | blue-500 |
| 成功 | #52c41a | green-500 |
| 警告 | #faad14 | yellow-500 |
| 错误 | #ff4d4f | red-500 |
| 文字主色 | #333333 | gray-800 |
| 文字次色 | #666666 | gray-500 |
| 背景色 | #f5f5f5 | gray-100 |

### 6.2 预约状态颜色

| 状态 | 颜色 | Badge类型 |
|------|------|-----------|
| 已预约 | 蓝色 | processing |
| 已完成 | 绿色 | success |
| 已取消 | 灰色 | default |

### 6.3 Ant Design组件推荐

| 场景 | 推荐组件 |
|------|----------|
| 表单 | Form, Input, Select, DatePicker |
| 列表 | Table, List, Card |
| 反馈 | Message, Modal, Notification |
| 导航 | Menu, Tabs, Breadcrumb |
| 数据展示 | Descriptions, Tag, Badge |
| 上传 | Upload, Dragger |

---

## 七、开发检查清单

### 7.1 功能清单

- [ ] 首页科室展示
- [ ] 患者注册（年龄验证）
- [ ] 患者登录
- [ ] 患者信息修改
- [ ] 患者注销
- [ ] 科室查询
- [ ] 医生查询（按科室）
- [ ] 医生排班查询
- [ ] 预约挂号
- [ ] 取消预约
- [ ] 我的预约列表
- [ ] 管理员登录
- [ ] 医生管理（CRUD）
- [ ] 排班管理（CRUD）
- [ ] 批量导入医生
- [ ] 批量导入排班
- [ ] 导出预约记录
- [ ] 生成月度报告

### 7.2 验证规则清单

- [ ] 姓名：最多20字符
- [ ] 密码：不少于4位
- [ ] 身份证号：18位数字
- [ ] 手机号：11位，1开头
- [ ] 性别：M或F
- [ ] 患者ID：10位数字
- [ ] 医生ID：8位数字
- [ ] 预约号：12位数字

---

## 八、启动命令

```bash
# 创建项目
npm create vite@latest frontend -- --template react

# 进入目录
cd frontend

# 安装依赖
npm install
npm install antd @ant-design/icons
npm install tailwindcss postcss autoprefixer
npm install react-router-dom
npm install axios
npm install zustand
npm install dayjs

# 初始化Tailwind
npx tailwindcss init -p

# 启动开发服务器
npm run dev
```

